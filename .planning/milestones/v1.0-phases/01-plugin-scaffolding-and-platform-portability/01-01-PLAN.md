---
phase: 01-plugin-scaffolding-and-platform-portability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - tsdown.config.ts
  - vitest.config.ts
  - .gitignore
  - .npmrc
  - openclaw.plugin.json
  - src/index.ts
  - src/vendor/yaml/index.ts
  - src/adapters/types.ts
  - src/adapters/detect.ts
  - src/adapters/index.ts
  - src/adapters/openclaw/index.ts
  - src/adapters/standalone/index.ts
  - src/activation/gate.ts
  - src/activation/cans-parser.ts
  - src/activation/cans-integrity.ts
  - src/activation/cans-schema.ts
  - src/audit/entry-schema.ts
  - src/audit/writer.ts
  - src/audit/pipeline.ts
  - src/audit/integrity-service.ts
  - src/cli/commands.ts
  - src/cli/init-command.ts
  - src/cli/status-command.ts
  - src/cli/io.ts
  - src/cli/prompts.ts
  - src/credentials/index.ts
  - src/credentials/types.ts
  - src/credentials/validator.ts
  - src/skills/index.ts
  - src/skills/types.ts
  - src/skills/loader.ts
  - src/skills/manifest-schema.ts
  - src/skills/integrity.ts
  - src/skills/version-pin.ts
  - src/onboarding/workspace-profiles.ts
  - src/onboarding/workspace-content.ts
  - src/onboarding/workspace-writer.ts
  - src/onboarding/engine.ts
  - src/onboarding/stages.ts
  - src/onboarding/defaults.ts
  - src/onboarding/review.ts
  - src/onboarding/cans-generator.ts
  - src/refinement/index.ts
  - src/refinement/types.ts
  - src/refinement/refinement-engine.ts
  - src/protocol/index.ts
  - src/protocol/types.ts
  - src/protocol/server.ts
  - src/neuron/index.ts
  - src/neuron/types.ts
  - src/neuron/client.ts
  - src/chart/types.ts
autonomous: true
requirements:
  - PLUG-01
  - PLUG-03
  - PLUG-04
  - PORT-01
  - PORT-02

must_haves:
  truths:
    - "pnpm build succeeds with zero runtime npm dependencies"
    - "Plugin manifest (openclaw.plugin.json) declares @careagent/patient-core with correct id, name, empty skills/commands/hooks"
    - "PlatformAdapter interface defines all 8 methods including registerHook"
    - "Duck-type detection correctly identifies OpenClaw API vs standalone"
    - "OpenClaw adapter wraps every API call in try/catch with graceful degradation"
    - "Standalone adapter provides no-op implementations with console logging"
    - "All stub modules export types and throw 'not yet implemented' from factory functions"
  artifacts:
    - path: "package.json"
      provides: "Zero-dep ESM package with exports map for 4 entry points"
      contains: "@careagent/patient-core"
    - path: "openclaw.plugin.json"
      provides: "OpenClaw plugin manifest"
      contains: "@careagent/patient-core"
    - path: "src/adapters/types.ts"
      provides: "PlatformAdapter interface with registerHook"
      exports: ["PlatformAdapter", "ToolCallHandler", "BootstrapHandler", "CliCommandConfig", "ServiceConfig", "SlashCommandConfig", "ToolCallEvent"]
    - path: "src/adapters/detect.ts"
      provides: "Duck-type platform detection"
      exports: ["detectPlatform"]
    - path: "src/adapters/openclaw/index.ts"
      provides: "OpenClaw adapter with try/catch wrapping"
      exports: ["createOpenClawAdapter"]
    - path: "src/adapters/standalone/index.ts"
      provides: "Standalone no-op adapter"
      exports: ["createStandaloneAdapter"]
    - path: "src/chart/types.ts"
      provides: "Patient-chart vault interface contract"
      exports: ["PatientChartVault", "ChartOperationResult"]
  key_links:
    - from: "src/adapters/openclaw/index.ts"
      to: "src/adapters/types.ts"
      via: "implements PlatformAdapter"
      pattern: "PlatformAdapter"
    - from: "src/adapters/standalone/index.ts"
      to: "src/adapters/types.ts"
      via: "implements PlatformAdapter"
      pattern: "PlatformAdapter"
    - from: "src/adapters/detect.ts"
      to: "src/adapters/openclaw/index.ts"
      via: "detection result determines which adapter is created"
      pattern: "detectPlatform"
    - from: "package.json"
      to: "tsdown.config.ts"
      via: "build script invokes tsdown which reads config"
      pattern: "tsdown"
---

<objective>
Scaffold the complete @careagent/patient-core project: config files, full directory structure mirroring provider-core, PlatformAdapter with duck-type detection, both adapter implementations, and all stub modules for future phases. Build must succeed with zero runtime npm dependencies.

Purpose: Establish the identical project foundation that provider-core uses, so all subsequent phases build on proven infrastructure. The adapter layer is the core portability mechanism -- everything else depends on it.

Output: Buildable TypeScript project with working adapters, plugin manifest, and all stub modules exporting types.
</objective>

<execution_context>
@/Users/medomatic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/medomatic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-scaffolding-and-platform-portability/01-CONTEXT.md
@.planning/phases/01-plugin-scaffolding-and-platform-portability/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project config files and install dependencies</name>
  <files>
    package.json
    tsconfig.json
    tsdown.config.ts
    vitest.config.ts
    .gitignore
    .npmrc
    openclaw.plugin.json
  </files>
  <action>
Create all project configuration files by mirroring provider-core exactly (reference: /Users/medomatic/Documents/Projects/provider-core/). Read provider-core's actual files to ensure exact match, then adapt only what CONTEXT.md specifies differently.

**package.json:** Copy provider-core's package.json structure. Change: `name` to `@careagent/patient-core`, `description` to "Patient-facing clinical agent with consent engine and secure channel". Keep identical: `type: "module"`, `engines.node: ">=22.12.0"`, all devDependencies (typescript ~5.7.0, tsdown ~0.20.0, vitest ~4.0.0, @vitest/coverage-v8 ~4.0.0, @sinclair/typebox ~0.34.0, yaml ^2.8.2), zero `dependencies` field, `openclaw` as optional peerDependency (>=2026.1.0), `exports` map for 4 entry points (., ./openclaw, ./standalone, ./core), `openclaw.extensions: ["./dist/index.js"]`, all scripts (build, dev, test, test:watch, test:coverage, typecheck, clean).

**tsconfig.json:** Exact copy from provider-core. ES2023 target, NodeNext module/moduleResolution, strict: true, all strict checks enabled.

**tsdown.config.ts:** Exact copy from provider-core. 4 entry points (src/index.ts, src/entry/openclaw.ts, src/entry/standalone.ts, src/entry/core.ts), ESM format, DTS, clean, sourcemap, external: ['openclaw', 'openclaw/*'].

**vitest.config.ts:** Exact copy from provider-core. Globals: true, include test/**/*.test.ts, V8 coverage provider, 80% thresholds for lines/branches/functions/statements, exclude src/vendor/**.

**.gitignore:** node_modules/, dist/, coverage/, *.tsbuildinfo, .DS_Store.

**.npmrc:** shamefully-hoist=false, strict-peer-dependencies=false.

**openclaw.plugin.json:** `id: "@careagent/patient-core"`, `name: "CareAgent"`, `description: "Patient-facing clinical agent with consent engine and secure channel"`, `version: "0.1.0"`, `configSchema: {}`, `skills: []`, `commands: []`, `hooks: []`.

After creating files, run `pnpm install` to install dependencies.
  </action>
  <verify>
Run `pnpm install` completes without errors. Verify `node_modules` directory exists. Verify `package.json` has zero `dependencies` field (only devDependencies and peerDependencies). Verify `cat package.json | grep '"type": "module"'` outputs the line. Run `pnpm typecheck` (will fail since no src files yet -- that is expected at this point).
  </verify>
  <done>
All 7 config files exist and match provider-core patterns. Dependencies installed. package.json declares zero runtime dependencies, 4 exports, openclaw as optional peer dep.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create PlatformAdapter interface, detection, and both adapter implementations</name>
  <files>
    src/adapters/types.ts
    src/adapters/detect.ts
    src/adapters/index.ts
    src/adapters/openclaw/index.ts
    src/adapters/standalone/index.ts
    src/vendor/yaml/index.ts
  </files>
  <action>
Read provider-core's actual adapter files (at /Users/medomatic/Documents/Projects/provider-core/src/adapters/) for exact patterns, then create patient-core versions.

**src/adapters/types.ts:** Mirror provider-core's PlatformAdapter interface exactly, then ADD one method: `registerHook(name: string, handler: (...args: unknown[]) => void): void`. Export all types: PlatformAdapter, ToolCallHandler, BootstrapHandler, CliCommandConfig, ServiceConfig, SlashCommandConfig, ToolCallEvent, DetectedPlatform. The ToolCallEvent must match provider-core's shape (event name, tool name, parameters, and a result callback or similar). Read provider-core to get the exact shape.

**src/adapters/detect.ts:** Mirror provider-core's detect.ts exactly. Export `detectPlatform(api: unknown): DetectedPlatform`. Duck-type check: `typeof raw?.registerCli === 'function' && typeof raw?.on === 'function'` returns 'openclaw', else 'standalone'. Also export `createAdapter(api: unknown): PlatformAdapter` that calls detectPlatform and returns the appropriate adapter.

**src/adapters/openclaw/index.ts:** Mirror provider-core's openclaw adapter exactly. Export `createOpenClawAdapter(raw: unknown): PlatformAdapter`. Every method wraps the raw API call in try/catch: getWorkspacePath (with full fallback chain: raw.workspaceDir > raw.config.workspaceDir > raw.context.workspaceDir > process.cwd()), onBeforeToolCall, onAgentBootstrap, registerCliCommand, registerBackgroundService, registerSlashCommand, log. For registerHook: maintain an internal `Map<string, Array<(...args: unknown[]) => void>>` and store handlers. Add a `triggerHook(name, ...args)` helper (not on the interface, exported separately or as a closure).

**src/adapters/standalone/index.ts:** Mirror provider-core's standalone adapter exactly. Export `createStandaloneAdapter(workspacePath?: string): PlatformAdapter`. All registration methods are no-ops. `log` uses console[level]. `getWorkspacePath` returns resolved path (param or process.cwd()). `registerHook` is a no-op.

**src/adapters/index.ts:** Re-export everything from types.ts, detect.ts. Re-export createOpenClawAdapter and createStandaloneAdapter.

**src/vendor/yaml/index.ts:** `export { parse as parseYAML, stringify as stringifyYAML } from 'yaml';`
  </action>
  <verify>
Run `pnpm typecheck` (will check adapter types compile). Verify src/adapters/types.ts exports PlatformAdapter with registerHook method. Verify src/adapters/detect.ts exports detectPlatform and createAdapter. Verify src/adapters/openclaw/index.ts has try/catch around every raw API call (grep for 'catch').
  </verify>
  <done>
PlatformAdapter interface defined with all 8 methods (7 from provider-core + registerHook). Duck-type detection works. OpenClaw adapter wraps all calls in try/catch. Standalone adapter provides no-ops. Vendor YAML re-exports parseYAML and stringifyYAML.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create all stub modules for future phases</name>
  <files>
    src/activation/gate.ts
    src/activation/cans-parser.ts
    src/activation/cans-integrity.ts
    src/activation/cans-schema.ts
    src/audit/entry-schema.ts
    src/audit/writer.ts
    src/audit/pipeline.ts
    src/audit/integrity-service.ts
    src/cli/commands.ts
    src/cli/init-command.ts
    src/cli/status-command.ts
    src/cli/io.ts
    src/cli/prompts.ts
    src/credentials/index.ts
    src/credentials/types.ts
    src/credentials/validator.ts
    src/skills/index.ts
    src/skills/types.ts
    src/skills/loader.ts
    src/skills/manifest-schema.ts
    src/skills/integrity.ts
    src/skills/version-pin.ts
    src/onboarding/workspace-profiles.ts
    src/onboarding/workspace-content.ts
    src/onboarding/workspace-writer.ts
    src/onboarding/engine.ts
    src/onboarding/stages.ts
    src/onboarding/defaults.ts
    src/onboarding/review.ts
    src/onboarding/cans-generator.ts
    src/refinement/index.ts
    src/refinement/types.ts
    src/refinement/refinement-engine.ts
    src/protocol/index.ts
    src/protocol/types.ts
    src/protocol/server.ts
    src/neuron/index.ts
    src/neuron/types.ts
    src/neuron/client.ts
    src/chart/types.ts
    src/index.ts
  </files>
  <action>
Read provider-core's actual stub modules (at /Users/medomatic/Documents/Projects/provider-core/src/) to see the exact pattern for each, then create patient-core versions. Each module follows one of two patterns:

**Pattern A — Type + factory stub:** Exports types and a factory function that returns an object whose methods throw `new Error('... not yet implemented (Phase N)')`. Used for: activation, audit, credentials, skills, onboarding, refinement, protocol, neuron.

**Pattern B — Re-export index:** Exports everything from sibling files. Used for: credentials/index.ts, skills/index.ts, refinement/index.ts, protocol/index.ts, neuron/index.ts.

Specific stubs:

- **src/activation/gate.ts:** ActivationGate class with `check()` method returning `{ active: false, document: null, reason: 'CANS schema not yet implemented (Phase 2)' }`. Read provider-core's gate.ts for exact interface shape.
- **src/activation/cans-parser.ts:** Export `parseCANS(content: string)` that throws 'not yet implemented (Phase 2)'.
- **src/activation/cans-integrity.ts:** Export `verifyIntegrity(content: string, hash: string): boolean` that throws 'not yet implemented (Phase 2)'.
- **src/activation/cans-schema.ts:** Export `CANSDocument` type and `validateCANS(data: unknown)` that throws 'not yet implemented (Phase 2)'. The CANSDocument type should be a minimal placeholder.
- **src/audit/**: Read provider-core's audit module for exact shapes. Create entry-schema.ts (TypeBox schema stub), writer.ts (hash-chained writer stub), pipeline.ts (AuditPipeline class with `log()` and `flush()` stubs), integrity-service.ts (background verifier stub).
- **src/cli/commands.ts:** `registerCLI(adapter, workspacePath, audit)` function stub that registers `careagent init` and `careagent status` commands. Read provider-core for exact pattern.
- **src/cli/init-command.ts, status-command.ts:** Stub handlers that log 'not yet implemented (Phase 4)'.
- **src/cli/io.ts, prompts.ts:** Terminal I/O abstraction stubs matching provider-core.
- **src/credentials/:** types.ts with CredentialValidator interface, validator.ts with stub factory, index.ts re-exports.
- **src/skills/:** types.ts with Skill interface, loader.ts with stub, manifest-schema.ts with TypeBox stub, integrity.ts and version-pin.ts with stubs. index.ts re-exports.
- **src/onboarding/workspace-profiles.ts:** Platform-specific file profiles for supplementation (PORT-03). Define profile shapes for openclaw, agents-standard, standalone. Content generation deferred to Phase 4 but the profile STRUCTURE must exist.
- **src/onboarding/:** Remaining files as 'not yet implemented (Phase 4)' stubs. Read provider-core for shapes.
- **src/refinement/:** types.ts, refinement-engine.ts factory stub, index.ts re-exports.
- **src/protocol/:** types.ts with ProtocolServer interface, server.ts factory stub, index.ts re-exports.
- **src/neuron/:** types.ts with NeuronClient interface, client.ts factory stub, index.ts re-exports.
- **src/chart/types.ts:** Patient-specific. Define `PatientChartVault` interface with `read(recordId: string): Promise<unknown>`, `write(recordId: string, data: unknown): Promise<ChartOperationResult>`, `checkAccess(recordId: string): Promise<boolean>`. Define `ChartOperationResult` with `success: boolean` and optional `error: string`.
- **src/index.ts:** Default export that re-exports the OpenClaw plugin registration function from `./entry/openclaw.js`. Read provider-core's index.ts for exact pattern. Use `export { default } from './entry/openclaw.js';` or similar.

IMPORTANT: For src/index.ts, it must re-export the register function as the default export. The OpenClaw entry point (src/entry/openclaw.ts) does not exist yet (created in Plan 02), so src/index.ts should import and re-export from it. Since Plan 02 creates the entry points, src/index.ts may need to reference a not-yet-created file. To avoid build errors, create a minimal placeholder src/entry/openclaw.ts that exports a default register function skeleton (just the export signature), which Plan 02 Task 1 will replace with the full implementation. This placeholder must accept `api: unknown` and do nothing (empty body).

Run `pnpm typecheck` after creating all stubs to verify everything compiles. Run `pnpm build` to verify tsdown produces artifacts. Fix any type errors.
  </action>
  <verify>
Run `pnpm typecheck` passes with zero errors. Run `pnpm build` succeeds. Verify `dist/` contains index.js, entry/openclaw.js, entry/standalone.js (placeholder), entry/core.js (placeholder). Verify all stub modules exist in their expected directories. Run `ls -R src/` and confirm directory structure matches the research document's recommended structure.
  </verify>
  <done>
Complete directory structure mirroring provider-core exists. All stub modules export types and factory stubs. pnpm build produces zero-error artifacts. The project compiles and builds end-to-end.
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` produces dist/ with all 4 entry points (index.js, entry/openclaw.js, entry/standalone.js, entry/core.js)
2. `pnpm typecheck` passes with zero errors
3. `cat package.json | node -e "const p=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.log('deps:', Object.keys(p.dependencies||{}).length)"` outputs `deps: 0`
4. `openclaw.plugin.json` has `id: "@careagent/patient-core"` and `skills: []`
5. `src/adapters/types.ts` contains `registerHook` method
6. `src/adapters/openclaw/index.ts` has try/catch around every raw API call
7. `src/adapters/detect.ts` uses duck-typing without importing openclaw
8. All stub modules exist and export their declared types
</verification>

<success_criteria>
A buildable @careagent/patient-core TypeScript project exists with zero runtime dependencies, correct plugin manifest, full PlatformAdapter layer (interface, detection, both implementations), and all stub modules for future phases. `pnpm build` and `pnpm typecheck` both pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-scaffolding-and-platform-portability/01-01-SUMMARY.md`
</output>
