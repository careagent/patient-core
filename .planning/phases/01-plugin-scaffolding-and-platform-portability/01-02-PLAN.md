---
phase: 01-plugin-scaffolding-and-platform-portability
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/entry/openclaw.ts
  - src/entry/standalone.ts
  - src/entry/core.ts
  - src/hardening/types.ts
  - src/hardening/engine.ts
  - src/hardening/index.ts
  - src/hardening/canary.ts
  - src/hardening/layers/tool-policy.ts
  - src/hardening/layers/exec-allowlist.ts
  - src/hardening/layers/cans-injection.ts
  - src/hardening/layers/docker-sandbox.ts
  - src/hardening/layers/consent-gate.ts
  - src/hardening/layers/data-minimization.ts
  - test/smoke.test.ts
  - test/unit/adapters/detect.test.ts
  - test/unit/adapters/openclaw/openclaw-adapter.test.ts
  - test/unit/adapters/standalone.test.ts
  - test/unit/hardening/hardening.test.ts
  - test/unit/hardening/canary.test.ts
  - test/unit/hardening/layers/tool-policy.test.ts
  - test/unit/hardening/layers/exec-allowlist.test.ts
  - test/unit/hardening/layers/cans-injection.test.ts
  - test/unit/hardening/layers/docker-sandbox.test.ts
  - test/unit/hardening/layers/consent-gate.test.ts
  - test/unit/hardening/layers/data-minimization.test.ts
  - test/integration/plugin.test.ts
  - test/fixtures/mock-api.ts
  - skills/.gitkeep
autonomous: true
requirements:
  - PLUG-02
  - PLUG-05
  - PORT-03
  - PORT-04

must_haves:
  truths:
    - "OpenClaw entry point (register function) accepts a mock API, creates adapter, starts audit, registers CLI, checks activation gate"
    - "Standalone entry point (activate function) returns constructed objects without requiring OpenClaw"
    - "Core entry point exports only types with zero side effects"
    - "Hardening engine runs all 6 layers in order; first-deny-wins semantics"
    - "consent-gate and data-minimization stubs return allow-all"
    - "Plugin degrades gracefully when hooks are unavailable (try/catch, warn, no crash)"
    - "Workspace file profiles define platform-specific supplementation shapes for openclaw, agents-standard, and standalone"
    - "All tests pass with vitest; smoke test confirms register function works with mock API"
  artifacts:
    - path: "src/entry/openclaw.ts"
      provides: "OpenClaw plugin registration entry point"
      exports: ["default"]
    - path: "src/entry/standalone.ts"
      provides: "Standalone activation entry point"
      exports: ["activate"]
    - path: "src/entry/core.ts"
      provides: "Pure type re-exports, no side effects"
      exports: ["PlatformAdapter", "CANSDocument"]
    - path: "src/hardening/engine.ts"
      provides: "Hardening layer pipeline orchestrator"
      exports: ["createHardeningEngine"]
    - path: "src/hardening/layers/consent-gate.ts"
      provides: "Allow-all consent gate stub (Phase 5)"
      exports: ["checkConsentGate"]
    - path: "src/hardening/layers/data-minimization.ts"
      provides: "Allow-all data minimization stub (Phase 5)"
      exports: ["checkDataMinimization"]
    - path: "test/smoke.test.ts"
      provides: "Smoke test: exports register function, accepts mock API"
      min_lines: 10
    - path: "test/integration/plugin.test.ts"
      provides: "Integration test: plugin lifecycle, manifest, registration"
      min_lines: 30
  key_links:
    - from: "src/entry/openclaw.ts"
      to: "src/adapters/detect.ts"
      via: "createAdapter call to detect platform and build adapter"
      pattern: "createAdapter"
    - from: "src/entry/openclaw.ts"
      to: "src/hardening/engine.ts"
      via: "creates and activates hardening engine when clinical mode active"
      pattern: "createHardeningEngine"
    - from: "src/entry/openclaw.ts"
      to: "src/activation/gate.ts"
      via: "checks activation gate to determine clinical mode"
      pattern: "ActivationGate"
    - from: "src/entry/openclaw.ts"
      to: "src/audit/pipeline.ts"
      via: "creates AuditPipeline for logging"
      pattern: "AuditPipeline"
    - from: "src/entry/openclaw.ts"
      to: "src/cli/commands.ts"
      via: "registers CLI commands"
      pattern: "registerCLI"
    - from: "src/hardening/engine.ts"
      to: "src/hardening/layers/*.ts"
      via: "orchestrates all layers in order"
      pattern: "checkConsentGate|checkDataMinimization|checkToolPolicy"
    - from: "src/index.ts"
      to: "src/entry/openclaw.ts"
      via: "default export re-export"
      pattern: "export.*default.*from.*openclaw"
    - from: "test/smoke.test.ts"
      to: "src/index.ts"
      via: "imports and tests the default export"
      pattern: "import.*index"
---

<objective>
Implement the three entry points (OpenClaw, standalone, core), hardening engine with all 6 layers (4 real + 2 allow-all stubs), and comprehensive test suite. This completes the plugin registration flow and ensures graceful degradation.

Purpose: The entry points are how the outside world interacts with patient-core. The hardening engine is the security pipeline that every tool call passes through. Tests prove the entire scaffold works end-to-end.

Output: Three working entry points, hardening pipeline, 80%+ test coverage, all tests passing.
</objective>

<execution_context>
@/Users/medomatic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/medomatic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-scaffolding-and-platform-portability/01-CONTEXT.md
@.planning/phases/01-plugin-scaffolding-and-platform-portability/01-RESEARCH.md
@.planning/phases/01-plugin-scaffolding-and-platform-portability/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement hardening engine with all 6 layers</name>
  <files>
    src/hardening/types.ts
    src/hardening/engine.ts
    src/hardening/index.ts
    src/hardening/canary.ts
    src/hardening/layers/tool-policy.ts
    src/hardening/layers/exec-allowlist.ts
    src/hardening/layers/cans-injection.ts
    src/hardening/layers/docker-sandbox.ts
    src/hardening/layers/consent-gate.ts
    src/hardening/layers/data-minimization.ts
  </files>
  <action>
Read provider-core's hardening module (at /Users/medomatic/Documents/Projects/provider-core/src/hardening/) for exact patterns, then create patient-core versions.

**src/hardening/types.ts:** Define `HardeningLayerResult` with `{ layer: string, allowed: boolean, reason: string }`. Define `HardeningEngine` interface with `activate(config: { cans: CANSDocument, adapter: PlatformAdapter, audit: AuditPipeline }): void` and a method to check tool calls. Define `ToolCallEvent` re-export or import from adapters/types.ts. Read provider-core for exact shape.

**src/hardening/engine.ts:** Export `createHardeningEngine()` returning a HardeningEngine. The engine holds a reference to the ordered layer pipeline. `activate()` stores the CANS document, adapter, and audit reference. The tool call check method runs all 6 layers in order (tool-policy, exec-allowlist, cans-injection, docker-sandbox, consent-gate, data-minimization). First deny wins: if any layer returns `allowed: false`, stop and return that result. Each layer call is logged via audit. Read provider-core for exact orchestration pattern.

**src/hardening/canary.ts:** Hook liveness canary. Detects if before_tool_call hooks are actually firing. If no hook fires within a configurable timeout after activation, log a warning. Read provider-core's canary.ts for exact pattern.

**Layers 1-4 (tool-policy.ts, exec-allowlist.ts, cans-injection.ts, docker-sandbox.ts):** Mirror provider-core's implementations exactly. Each exports a check function that takes a ToolCallEvent and CANSDocument, returns HardeningLayerResult. Read provider-core for exact logic. These are real implementations, not stubs.

**Layers 5-6 (consent-gate.ts, data-minimization.ts):** Allow-all stubs per CONTEXT.md. Each exports a check function with same signature. Returns `{ layer: LAYER_NAME, allowed: true, reason: 'stub -- [layer] not yet implemented (Phase 5)' }`. These are explicitly stub implementations that will be replaced in Phase 5.

**src/hardening/index.ts:** Re-export createHardeningEngine, types, canary.
  </action>
  <verify>
Run `pnpm typecheck` passes. Verify consent-gate.ts and data-minimization.ts return `allowed: true` (grep for 'allowed: true'). Verify engine.ts imports all 6 layers. Verify tool-policy.ts, exec-allowlist.ts, cans-injection.ts, docker-sandbox.ts have real logic (not just stubs).
  </verify>
  <done>
Hardening engine with all 6 layers exists. Layers 1-4 have real enforcement logic matching provider-core. Layers 5-6 are allow-all stubs. Pipeline runs in order with first-deny-wins semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement three entry points and update index.ts</name>
  <files>
    src/entry/openclaw.ts
    src/entry/standalone.ts
    src/entry/core.ts
    src/index.ts
    skills/.gitkeep
  </files>
  <action>
Read provider-core's entry points (at /Users/medomatic/Documents/Projects/provider-core/src/entry/) for exact patterns, then create patient-core versions.

**src/entry/openclaw.ts:** Replace the placeholder from Plan 01. Export default `register(api: unknown): void`. Flow mirrors provider-core exactly:
1. `const adapter = createAdapter(api)` — duck-type detect and create appropriate adapter
2. `const workspacePath = adapter.getWorkspacePath()`
3. `const audit = new AuditPipeline(workspacePath)` (or factory, match provider-core)
4. `registerCLI(adapter, workspacePath, audit)` — register careagent init and careagent status
5. `const gate = new ActivationGate(workspacePath, (entry) => audit.log(entry))`
6. `const result = gate.check()`
7. If not active: log reason, return early
8. If active: `const engine = createHardeningEngine()`, `engine.activate({ cans: result.document, adapter, audit })`, register background services (canary, integrity verification), load skills

All of step 7-8 will mostly log and return early since activation gate stub returns inactive (Phase 2 concern). The STRUCTURE must be complete for when the activation gate is implemented. Read provider-core to match exact control flow. Each major operation should be wrapped in its own try/catch for graceful degradation (PLUG-05).

**src/entry/standalone.ts:** Export `activate(workspacePath?: string)`. Creates standalone adapter, then follows same flow as openclaw but returns constructed objects instead of registering with API. Returns `{ adapter, audit, gate, engine? }`. Read provider-core for exact return shape.

**src/entry/core.ts:** Pure type re-exports only. NO side effects. Export PlatformAdapter, CANSDocument, HardeningLayerResult, AuditEntry (or whatever the audit entry type is), and other key types from across the codebase. Read provider-core's core.ts for which types to re-export. The goal: consumers who only need types can import from `@careagent/patient-core/core` without triggering any file system, audit, or adapter creation.

**src/index.ts:** Update to properly re-export from entry/openclaw.ts: `export { default } from './entry/openclaw.js';`. This is the default OpenClaw plugin export.

**skills/.gitkeep:** Empty file to create the skills/ directory (empty per CONTEXT.md; skills come in Phase 7).

After implementation, verify `pnpm build` produces all 4 dist outputs and `pnpm typecheck` passes.
  </action>
  <verify>
Run `pnpm build` succeeds. Verify `dist/index.js`, `dist/entry/openclaw.js`, `dist/entry/standalone.js`, `dist/entry/core.js` all exist. Run `pnpm typecheck` passes. Verify `src/entry/core.ts` has NO imports from modules with side effects (no audit, no fs, no adapter creation).
  </verify>
  <done>
Three entry points implemented matching provider-core patterns. OpenClaw entry registers plugin with full lifecycle (adapter -> audit -> CLI -> gate -> hardening). Standalone entry returns objects. Core entry exports only types. Build produces all 4 outputs.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create comprehensive test suite</name>
  <files>
    test/smoke.test.ts
    test/fixtures/mock-api.ts
    test/unit/adapters/detect.test.ts
    test/unit/adapters/openclaw/openclaw-adapter.test.ts
    test/unit/adapters/standalone.test.ts
    test/unit/hardening/hardening.test.ts
    test/unit/hardening/canary.test.ts
    test/unit/hardening/layers/tool-policy.test.ts
    test/unit/hardening/layers/exec-allowlist.test.ts
    test/unit/hardening/layers/cans-injection.test.ts
    test/unit/hardening/layers/docker-sandbox.test.ts
    test/unit/hardening/layers/consent-gate.test.ts
    test/unit/hardening/layers/data-minimization.test.ts
    test/integration/plugin.test.ts
  </files>
  <action>
Read provider-core's tests (at /Users/medomatic/Documents/Projects/provider-core/test/) for patterns, then create patient-core test suite.

**test/fixtures/mock-api.ts:** Export `createMockAPI(workspacePath: string)` that returns a mock OpenClaw API object. Must have: `workspaceDir`, `registerCli` (records calls, invokes callback with mock program), `registerService` (records), `registerCommand` (records), `on` (records event+handler), `log` (records), and a `calls` array for assertions. Also export `createMinimalAPI(workspacePath: string)` with only workspaceDir (no methods) for degradation testing.

**test/smoke.test.ts:** 2 tests: (1) default export is a function, (2) register(mockAPI) does not throw. Use createMockAPI from fixtures. Uses temp directory via `mkdtempSync`.

**test/unit/adapters/detect.test.ts:** Test detectPlatform: full mock API returns 'openclaw', empty object returns 'standalone', null returns 'standalone', object with only registerCli but no on returns 'standalone'.

**test/unit/adapters/openclaw/openclaw-adapter.test.ts:** Test createOpenClawAdapter: (1) getWorkspacePath returns correct path from each fallback level, (2) onBeforeToolCall registers handler via raw.on, (3) registerCliCommand calls raw.registerCli, (4) log calls raw.log, (5) graceful degradation: adapter created with minimal API (no methods) does not throw on any method call, just logs warnings, (6) registerHook stores handler in internal map.

**test/unit/adapters/standalone.test.ts:** Test createStandaloneAdapter: (1) platform is 'standalone', (2) getWorkspacePath returns provided path or process.cwd(), (3) all registration methods are no-ops (call without error), (4) log outputs to console.

**test/unit/hardening/hardening.test.ts:** Test createHardeningEngine: (1) activate stores config, (2) tool call check runs all layers, (3) first deny wins (mock a layer returning denied), (4) all-allow returns allowed.

**test/unit/hardening/canary.test.ts:** Test canary: (1) warns if no hook fires within timeout.

**test/unit/hardening/layers/*.test.ts:** One test file per layer. For layers 1-4 (tool-policy, exec-allowlist, cans-injection, docker-sandbox): test with valid input returns expected result, test with denied input returns denied. For layers 5-6 (consent-gate, data-minimization): test returns `allowed: true` with stub reason.

**test/integration/plugin.test.ts:** Full lifecycle test: (1) create mock API with temp dir, (2) call register(mockAPI), (3) verify CLI commands registered (careagent init, careagent status), (4) verify activation gate checked, (5) verify no crash. Test degradation: (6) call register with minimal API (no methods), verify no throw, (7) verify warning logged about missing hooks.

Target: `pnpm test` passes all tests. `pnpm test:coverage` shows 80%+ on lines, branches, functions, statements. If coverage is below 80%, add additional tests for uncovered branches (especially error handling paths in the OpenClaw adapter and hardening engine).
  </action>
  <verify>
Run `pnpm test` -- all tests pass. Run `pnpm test:coverage` -- 80%+ on all 4 metrics (lines, branches, functions, statements). If below 80%, add tests and re-run until threshold met.
  </verify>
  <done>
All tests pass. Coverage is 80%+ across lines, branches, functions, statements. Smoke test confirms register works with mock API. Integration test confirms full plugin lifecycle. Unit tests cover adapters, detection, hardening engine, all 6 hardening layers, and canary. Graceful degradation tested with minimal API objects.
  </done>
</task>

</tasks>

<verification>
1. `pnpm test` passes all tests (smoke, unit, integration)
2. `pnpm test:coverage` shows 80%+ on lines, branches, functions, statements
3. `pnpm build` produces all 4 dist entry points
4. `pnpm typecheck` passes with zero errors
5. OpenClaw entry point: register(mockAPI) completes without error, registers CLI, checks gate
6. Standalone entry point: activate() returns { adapter, audit, gate } without OpenClaw
7. Core entry point: importing does NOT trigger file system or audit side effects
8. Hardening: consent-gate and data-minimization return allow-all; tool-policy and exec-allowlist enforce real checks
9. Degradation: register() with minimal API (no methods) does not crash, logs warnings
10. Workspace profiles define shapes for openclaw, agents-standard, standalone platforms
</verification>

<success_criteria>
All three entry points work correctly. Hardening pipeline runs 6 layers with first-deny-wins. Plugin gracefully degrades when OpenClaw hooks are unavailable. Test suite passes with 80%+ coverage. The entire @careagent/patient-core plugin can be installed and initialized without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-scaffolding-and-platform-portability/01-02-SUMMARY.md`
</output>
