---
phase: 03-audit-pipeline
plan: 02
type: tdd
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/audit/pipeline.ts
  - test/unit/audit/pipeline.test.ts
autonomous: true
requirements:
  - AUDT-02
  - AUDT-03
  - AUDT-06

must_haves:
  truths:
    - "Every patient action logged with full context (action, actor, outcome, details) via audit.log()"
    - "Bilateral correlation IDs enable cross-system audit trail linking between patient and provider"
    - "Audit entries contain references and metadata, never raw health data content"
    - "Pipeline exposes flush() for callers needing persistence guarantees"
    - "Pipeline enriches entries with schema_version, timestamp, session_id, trace_id automatically"
  artifacts:
    - path: "src/audit/pipeline.ts"
      provides: "High-level audit API with session management, bilateral correlation, and async flush"
      exports: ["AuditPipeline", "AuditLogInput"]
      min_lines: 60
    - path: "test/unit/audit/pipeline.test.ts"
      provides: "TDD test suite for AuditPipeline"
      min_lines: 80
  key_links:
    - from: "src/audit/pipeline.ts"
      to: "src/audit/writer.ts"
      via: "AuditWriter instance for actual write operations"
      pattern: "new AuditWriter"
    - from: "src/audit/pipeline.ts"
      to: "node:crypto"
      via: "randomUUID() for session_id, trace_id, correlation_id"
      pattern: "randomUUID"
    - from: "src/audit/pipeline.ts"
      to: "node:fs"
      via: "mkdirSync for .careagent directory creation"
      pattern: "mkdirSync"
---

<objective>
Implement the AuditPipeline using TDD -- the high-level API that all 15+ call sites depend on. This replaces the Phase 1 stub while preserving the exact API surface.

Purpose: AuditPipeline is the public audit API. Entry points, hardening engine, canary, and CLI all call `audit.log()`. This plan implements session management, trace correlation, entry enrichment, bilateral correlation ID support, and flush passthrough. All existing try/catch wrappers around `audit.log()` will start receiving real audit entries instead of thrown errors.

Output: Working AuditPipeline with session management, bilateral correlation, flush(), and comprehensive test suite.
</objective>

<execution_context>
@/Users/medomatic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/medomatic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-audit-pipeline/03-RESEARCH.md
@.planning/phases/03-audit-pipeline/03-01-SUMMARY.md

@src/audit/pipeline.ts
@src/audit/writer.ts
@src/audit/entry-schema.ts
@src/entry/openclaw.ts
@src/entry/standalone.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD AuditPipeline with session management, bilateral correlation, and flush</name>
  <files>src/audit/pipeline.ts, test/unit/audit/pipeline.test.ts</files>
  <action>
**RED phase:** Create `test/unit/audit/pipeline.test.ts` with failing tests covering:

1. **Construction:**
   - Creates `.careagent/` directory under workspace path if it does not exist
   - Generates a session_id (UUIDv4 format) if none provided
   - Uses provided session_id if given in constructor

2. **log() entry enrichment:**
   - Adds `schema_version: '1'` to every entry
   - Adds `timestamp` (ISO 8601 string) to every entry
   - Adds `session_id` from pipeline's session to every entry
   - Adds `trace_id` (generated UUIDv4) if not provided in input
   - Uses provided `trace_id` if given in input
   - Defaults `actor` to `'system'` if not provided
   - Passes through all optional fields (target, action_state, details, blocked_reason, blocking_layer, correlation_id, summary)
   - Does NOT include optional fields that are undefined (no `target: undefined` in JSON)

3. **logBlocked() convenience:**
   - Calls log() with `outcome: 'denied'` and `actor: 'system'`
   - Passes through action, target, blocked_reason, blocking_layer, action_state, details

4. **Bilateral correlation (AUDT-03):**
   - `createCorrelationId()` returns a UUIDv4 string
   - `correlation_id` in AuditLogInput is passed through to the entry
   - Multiple entries can share the same correlation_id (bilateral pair)

5. **Session and trace management:**
   - `getSessionId()` returns the session ID used for all entries
   - `createTraceId()` returns a new UUIDv4 each call
   - Trace IDs are distinct from session IDs

6. **Flush and chain verification passthrough:**
   - `flush()` returns a Promise (delegates to writer.flush())
   - `verifyChain()` returns chain verification result (delegates to writer.verifyChain())
   - After `log()` + `flush()`, entries are persisted to disk

7. **dispose() cleanup:**
   - `dispose()` calls writer.dispose() to clear timers

8. **Data safety (AUDT-06):**
   - Verify that entries logged from call patterns matching existing entry points (e.g., `action: 'activation_check', details: { reason: '...' }`) produce valid JSONL in the log file

Test fixtures: Use `node:fs/promises mkdtemp` for temp workspaces, read AUDIT.log after flush to verify contents, clean up in `afterEach`.

**GREEN phase:** Implement `src/audit/pipeline.ts`:

Replace the stub with working implementation. **Preserve the existing `AuditLogInput` interface** but extend it:

```typescript
export interface AuditLogInput {
  action: string;
  actor?: 'agent' | 'patient' | 'provider' | 'system';
  target?: string;
  outcome: 'allowed' | 'denied' | 'escalated' | 'error' | 'active' | 'inactive';
  action_state?: ActionStateType;
  details?: Record<string, unknown>;
  blocked_reason?: string;
  blocking_layer?: string;
  trace_id?: string;
  correlation_id?: string;   // NEW: bilateral audit correlation
  summary?: string;          // NEW: human-readable summary
}
```

Key implementation:
- **Constructor(workspacePath, sessionId?):** Create `.careagent/` dir via `mkdirSync({ recursive: true })`. Construct `AuditWriter` with path to `AUDIT.log`. Generate session_id via `randomUUID()` if not provided.
- **`log(input)`:** Build entry object with explicit field ordering. Use conditional spread for optional fields (match provider-core pattern: `...(input.target !== undefined && { target: input.target })`). Call `this.writer.append(entry)`.
- **`logBlocked(input)`:** Call `this.log()` with `outcome: 'denied'`, `actor: 'system'`, and the input fields.
- **`flush()`:** Return `this.writer.flush()`.
- **`verifyChain()`:** Return `this.writer.verifyChain()`.
- **`getSessionId()`:** Return `this.sessionId`.
- **`createTraceId()`:** Return `randomUUID()`.
- **`createCorrelationId()`:** Return `randomUUID()`.
- **`dispose()`:** Call `this.writer.dispose()`.

**IMPORTANT:** The `actor` field in `AuditLogInput` must now include `'provider'` to match the updated schema from Plan 01.

**IMPORTANT:** Entry enrichment must use explicit property ordering (same as writer) to ensure deterministic JSON serialization.

**REFACTOR phase:** Clean up, ensure all tests pass.
  </action>
  <verify>
Run `npx vitest run test/unit/audit/pipeline.test.ts` -- all pipeline tests pass.
Run `npx vitest run` -- all existing 166+ tests still pass (no regressions from removing stub throws).
Verify that existing call sites (entry points, hardening engine, canary) no longer trigger try/catch error paths since pipeline.log() no longer throws.
  </verify>
  <done>
AuditPipeline replaces the Phase 1 stub with working implementation. All 15+ call sites now produce real audit entries instead of caught errors. Session management, bilateral correlation, and flush work correctly. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass (existing + new pipeline tests)
2. `npx vitest run --coverage` -- verify audit/pipeline.ts has >80% coverage
3. Bilateral correlation: create pipeline, log two entries with same correlation_id, flush, verify both entries in file share the ID
4. Existing call site compatibility: entry point tests still pass (try/catch no longer triggered but code path is still valid)
</verification>

<success_criteria>
- AuditPipeline replaces stub with working implementation
- All 15+ existing call sites now produce real audit entries (no more caught errors)
- Session management: every entry has consistent session_id
- Bilateral correlation: createCorrelationId() and correlation_id passthrough work
- flush() persists buffered entries to disk
- verifyChain() delegates to writer and returns structured result
- AuditLogInput extended with correlation_id, summary, and provider actor
- All existing tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/03-audit-pipeline/03-02-SUMMARY.md`
</output>
